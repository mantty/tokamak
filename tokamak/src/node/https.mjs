import { Agent, Server, createServer, get, globalAgent, request } from "./http.mjs";

export { Agent, Server, createServer, get, globalAgent, request };
export default { Agent, Server, createServer, get, globalAgent, request };
